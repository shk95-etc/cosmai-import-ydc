# STATE — facts that cannot be computed, and approval boundaries

**Boot order is in `AGENTS.md`, the work is in issues (`tool/issue ready`), and the operational facts that can be computed are printed by `tool/status`.** This file keeps only what those three cannot give: intent (what must be running), approval boundaries, rollback procedures. It is updated in the commit that closes the `ops` issue changing what runs. History is git.

## 2. Facts (not computable) — §1 (boot) is `AGENTS.md`
- **The new stack** (`stack/docker-compose.yml`) must be running: `cosmai-{analyze, collector-commerce, collector-naver, collector-youtube-work, collector-youtube-flatten, portal}`. `collector-youtube-watch` sits behind `profiles: ["youtube-watch"]` — the condition for restarting it is #39.
- **Old-stack remnants (kept running until #181)**: postgrest ×1 · data-portal · trend-radar-dashboard · tubedepth-api — they reach the database through the transition alias `shared-postgres` on `db-net`. The `shared-postgres` container itself is gone (#177, 2026-09-03); the cosmai-old containers ×4, their `postgrest-cosmai` and the two images are gone (#18, 2026-09-04). **Must be stopped**: trend-radar-collector · tubedepth-worker · tubedepth-flatten.
- The base of the images `cosmai-needs:local` and `cosmai-needs-cron:local` must be **trixie / OpenSSL ≥ 3.5** — bookworm (3.0) is blocked by the Cloudflare challenge on the oliveyoung review API (measured A/B, #35). `tests/stack/test_image_tls_stack.py` pins the lower bound.
- `stack/.env` must exist (paths only, no secret values). Without it compose dies on an unset `COSMAI_SECRET_FILE_HOST`.
- The browser profiles `var/browser-profiles/{oliveyoung,glowpick}` are read by `collector-commerce` as a bind mount (`user: "1000:1000"`, `HOME=/tmp`). A human creates them with `cosmai login`.
- **Production DB data directory**: `../service/service-data/postgres` (relative to the repo root; the parent of `18/docker`, postgres:18 layout). The old fleet's `shared-postgres` container died on 2026-08-28 and its compose definition is gone (#176); #177 binds this directory into the `postgres` service of `stack/docker-compose.yml` through the required `COSMAI_PG_DATA_DIR` in `stack/.env` (absolute host path, no secret). Apply only from the main checkout (D7).
- DB `cosmai-postgres` (service `postgres` of `stack/docker-compose.yml`, owned by this repo since #177) 127.0.0.1:5434, database `app` (trend_radar · tubedepth · needs). Database `cosmai` (cosmai-old) was dumped (`pg_dump -Fc`, kept beside the postgres data directory under `backups/`) and dropped on 2026-09-04 (#18); its schema stays in `contracts/ddl/current/cosmai.cosmai.sql` as a record. Superuser `platform`. The collector role passwords differ per schema (`TREND_RADAR_DB_RUNTIME` · `TUBEDEPTH_DB_RUNTIME`) — do not share them.
- Analysis: daily 05:00 UTC `cosmai analyze all` (rules) + an 08:00 UTC gemma4 incremental pass (`analyze polarity --impl ollama:gemma4:latest --missing`, #32). `OWNERS` holds 27 scopes — the pre-block (since=ALWAYS) and the 26 of #33 (since=2026-08) — and a rules run neither erases nor overwrites an owner's (scope, month) (`analysis/polarity/ownership.py`). The fork's `retrieval embed` avoids the GPU window 08:00–16:00 UTC.
- Secret key names: `contracts/secrets.md`. The values are written nowhere.
- DDL numbers: upstream 006–019, fork 020– (`contracts/versioning.md`).

## 3. Boundaries
- **Standing approval (2026-08-23, the ledger is the closed #16; rule changes go to #74 `[rules]`)**: applying DDL to production (additive only — DROP, type changes and data loss excluded) · LLM (`CLAUDE_API_KEY`, hard stop $10, sending review and comment text is OK) · push, issues, gh · rebuilding and recreating the new stack's containers (`cosmai-*`) (the session does it directly, `up -d --force-recreate`). Production DB migrate/seed/UPDATE, running `analyze`, and writing the secret file are done **by the coordinator session itself, one command at a time** (a subagent dispatch is blocked by the classifier). Production DB actions (migrations, views, seeds) run only from the upstream checkout by the coordinator session (D7, #192).
- **Approval every time**: rebuilding, stopping or restarting an old-stack container (postgrest · data-portal · dashboard · tubedepth-api; `cosmai-postgres` is the new stack but its restart and the first start on a data directory are approval items too) · anything of the `DROP` kind · a budget increase. Per-case approval is recorded in the comments of the relevant `ops`/`decision` issue, which are the ledger.
- **Evaluation before main (2026-09-05, #229 R3)**: the user's evaluation is required only for a change that touches production (DDL · seeds · views · `stack/` · `db/migrate.sh`), a rule site (AGENTS.md · `.claude/agents/` · `tool/checks/` · `.githooks/`), or one a reviewer escalates — grade A and waves alike; everything else the coordinator merges after its one review round (grade B) or on its green gate with a sampled review afterwards (grade C outside `contracts/`).
- **Deploy condition (2026-09-05, #228 D5′)**: `tool/stack-build` and a `db/migrate.sh` apply take only a tree whose class-A run is green, enforced by `tool/checks/deploy-gate` (#233) — the CI check run on the commit, or the local tested-tree record when `gh` cannot answer. `COSMAI_FORCE_DEPLOY=1` overrides the refusal with a reason written to the ops issue's journal. Main may carry a red unrelated to any merged change for at most a day and is fixed forward; production never does.
- **Forbidden**: modifying an archived old repository (editing the local stack and data-portal configuration is the exception, with no push) · extending the old cosmai platform · printing secret values or committing `.env` · `--no-verify` and force push · a UA that impersonates a browser or a spoofed TLS fingerprint (a collector uses a UA that identifies itself — `collectors/commerce/contract.py`; the reason for the current value is #35).

## 4. Relations
- **The relationship with architect (the design channel) was cut on 2026-08-25.** Seed inputs move inside the repository (#79). One contact point remains, an **inbox**: a proposal from outside arrives as an issue labelled `memo` + `external` and is either promoted to a channel issue or closed within one session. There is no approval gate, no document mirroring, no periodic sync. `contracts/` is canonical, and the old design documents are a birth record that does not follow revisions.
- **The fork `cosmai-import-ydc`**: the ledger of the ydc absorption is the fork's issues. It reaches upstream only as a PR (#83). Shared resources (GPU · production DB · `needs_runtime` 12 connections) go into the `Resources:` line of the start comment, and `tool/issue ready` reads both repos together. Each repo's worktree root is derived from its clone directory (D1, `AGENTS.md`). The fork does not edit this file.
- **Operational know-how** (`docker start` uses the old image · locks and stale code · `stack/.env` · one GPU · containers left over by the suite) is in `stack/README.md`.
